import { AlertStatus } from "@chakra-ui/react";

export const titles = new Map<string, string>([]);

export const descriptions = new Map<string, string>([]);

export const types = new Map<string, AlertStatus>([]);
